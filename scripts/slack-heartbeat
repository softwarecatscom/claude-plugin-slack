#!/usr/bin/env bash
# Shell wrapper for slack-heartbeat.py
# Runs the Python heartbeat script via uv for dependency management.
#
# Usage: slack-heartbeat [--maintenance [DURATION]] [CHANNEL_ID]
#   CHANNEL_ID  — channel containing the pinned status thread (default: from slack.conf)
#   --maintenance [DURATION] — signal maintenance mode (skipped by peer watchdog)

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

exec uv run --no-project "${SCRIPT_DIR}/slack-heartbeat.py" "$@"
